load("@npm//@bazel/typescript:index.bzl", "ts_project")
load("@build_bazel_rules_nodejs//:index.bzl", "nodejs_binary")

ts_project(
    name = "lib",
    srcs = glob(["*.ts"]),
    tsconfig = "//:tsconfig.json",
    deps = [
        "//trumid/ats/common:math",
        "@npm//@nestjs/common",
        "@npm//@nestjs/core",
    ],
)

nodejs_binary(
    name = "addition",
    data = [
        ":lib",
    ],
    entry_point = ":main.ts",
    link_workspace_root = True,
)
